


$("#new_product").validate({

//adding rule
  rules: {
  // name is required with max of 20 and min of 2
  "product[name]":{
    required: true,
    maxlength: 20,
    minlength: 2
  },


  // price is required with min value of 0
  "product[price]":{
    required: true,
    min: 0
  },

    // veg_nveg is required
  "product[veg_nveg]":{
    required: true
  },

    // category is required
  "product[category]":{
    required: true
  },

    // portion is required
  "product[portion]":{
    required: true
  }

  },
 
  messages: {
    name:{
      required: "Product name is required.",
      maxlength: "Product name must be less than 20",
      minlength: "Product name must be more than 2"
    },

    price:{
      required: "Product cost is required.",
      minlength: "Product cost must be more than 0"
    },


        veg_nveg:{
      required: "Answer is required.",
    },

        category:{
      required: "Product category is required."
    },


        portion:{
      required: "Product portion is required."
    }
  }
});